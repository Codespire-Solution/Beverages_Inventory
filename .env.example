# PostgreSQL connection string
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/beverage_inventory?schema=public"
# Secret for signing login tokens — set a long random value in production
JWT_SECRET="replace-with-a-long-random-string"
